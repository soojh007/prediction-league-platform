# Generated by Django 4.2.30 on 2026-07-13 06:00

from django.db import migrations, models
from django.utils.text import slugify


def populate_slugs(apps, schema_editor):
    PrivateLeague = apps.get_model('leagues', 'PrivateLeague')
    used_slugs = set()

    for league in PrivateLeague.objects.order_by('id'):
        if league.name == '2026-27 EPL Prediction League':
            base_slug = 'epl2627'
        else:
            base_slug = slugify(league.name)[:70] or f'league-{league.id}'

        slug = base_slug
        counter = 2
        while slug in used_slugs or PrivateLeague.objects.filter(slug=slug).exclude(pk=league.pk).exists():
            suffix = f'-{counter}'
            slug = f'{base_slug[:80 - len(suffix)]}{suffix}'
            counter += 1

        league.slug = slug
        league.save(update_fields=['slug'])
        used_slugs.add(slug)


class Migration(migrations.Migration):

    dependencies = [
        ('leagues', '0001_initial'),
    ]

    operations = [
        migrations.AddField(
            model_name='privateleague',
            name='slug',
            field=models.SlugField(blank=True, max_length=80, null=True),
        ),
        migrations.RunPython(populate_slugs, migrations.RunPython.noop),
        migrations.RunSQL(
            sql='DROP INDEX IF EXISTS leagues_privateleague_slug_9d6a99c8_like;',
            reverse_sql=migrations.RunSQL.noop,
        ),
        migrations.AlterField(
            model_name='privateleague',
            name='slug',
            field=models.SlugField(blank=True, max_length=80, unique=True),
        ),
    ]
